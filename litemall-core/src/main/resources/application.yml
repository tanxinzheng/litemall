spring:
  profiles:
    active: core, db
  messages:
    encoding: UTF-8
  http:
    multipart:
      maxFileSize: 100Mb
      maxRequestSize: 1000Mb

logging:
  level:
    root:  ERROR
    org.springframework:  ERROR
    org.mybatis:  ERROR
    org.linlinjava.litemall.core:  DEBUG